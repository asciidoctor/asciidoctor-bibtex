source 'https://rubygems.org'

gemspec

gem 'uri', '0.10.0.2' if ((Gem::Version.new RUBY_VERSION).segments.slice 0, 2) == [2, 7]
gem 'date', '3.0.3' if ((Gem::Version.new RUBY_VERSION).segments.slice 0, 2) == [2, 7]
gem 'date', '3.3.4' if ((Gem::Version.new RUBY_VERSION).segments.slice 0, 2) == [3, 3]
